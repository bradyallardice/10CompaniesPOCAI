pandas>=1.5.0
psycopg2-binary>=2.9.0
regex>=2022.0.0
langdetect>=1.0.9
tqdm>=4.64.0
python-dotenv>=0.19.0
openai>=1.0.0
anthropic>=0.3.0
numpy>=1.21.0
fuzzywuzzy>=0.18.0
python-levenshtein>=0.12.0
pyarrow>=12.0.0
sentence-transformers>=2.2.0
torch>=2.0.0
openpyxl>=3.0.0
scipy>=1.9.0
scikit-learn>=1.2.0
xlrd>=2.0.1
hf_transfer>=0.1.6
huggingface-hub>=0.16.0

# PyTorch GPU notes (RTX 5090 / SM120):
# - If you see "no kernel image is available", install a newer CUDA build of PyTorch
#   that supports SM120 (e.g., latest cu128/cu13x from PyTorch).
# - Confirm with: python - <<'PY'\nimport torch; print(torch.version.cuda, torch.cuda.get_device_capability())\nPY

# FAISS (optional, for Stage 4 --use-faiss mode)
# Install ONE of the following based on your system:
#   - For GPU (CUDA required): pip install faiss-gpu
#   - For CPU only: pip install faiss-cpu
# Note: faiss-gpu and faiss-cpu conflict, only install one
